import React from 'react'
import './TasksInterface.scss'

const TasksInterface = (props) => (
  <div className="tasks-interface">
    This is where tasks can be selected, reordered or completed. Completed tasks from this list can be toggled here. 
  </div>
)

export default TasksInterface