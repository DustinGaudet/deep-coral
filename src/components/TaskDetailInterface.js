import React from 'react'
import './TaskDetailInterface.scss'

const TaskDetailInterface = (props) => (
  <div className="task-detail-interface">
    Task details will be editable here.
  </div>
)

export default TaskDetailInterface